var util = require('util'),
    model = require('./model.js');

var User = function(row) {
    User.super_.call(this, row);
};
util.inherits(User, model);
User.tableName = "user";

var User = function(obj) {

};